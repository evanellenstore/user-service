server:
  port: 2030

spring:
  application:
    name: user-service

  datasource:
    #url: jdbc:mysql://localhost:3306/user_db
    url: jdbc:mysql://localhost:3306/store_db
    username: root
    password: ellenverma

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
